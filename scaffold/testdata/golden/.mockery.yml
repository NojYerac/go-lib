packages:
  github.com/example/example/data:
    config:
      dir: ./mocks/data
      with-expecter: True
      resolve-type-alias: False
      issue-845-fix: True
      include-regex: "^(DataSource)$"
